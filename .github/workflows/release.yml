name: release-desktop

# Build per-OS desktop client bundles and publish them to a GitHub Release.
# Cut a release by pushing a tag:  git tag v0.1.0 && git push origin v0.1.0
# Use "Run workflow" (workflow_dispatch) to build without releasing.
#
# This builds the desktop client only (see standoff.spec). The Android APK is
# built separately by android.yml.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

concurrency:
  group: release-desktop-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            asset: standoff-windows.zip
          - os: macos-latest
            asset: standoff-macos.zip
          - os: ubuntu-latest
            asset: standoff-linux.zip
    steps:
      - name: Checkout (with pygamine submodule)
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Set up uv + Python
        # setup-uv publishes no floating major tag; pin an exact version.
        uses: astral-sh/setup-uv@v8.2.0
        with:
          python-version: "3.12"

      - name: Install dependencies (incl. build group)
        run: uv sync --group build

      - name: Build bundle with PyInstaller
        env:
          # Headless build agents have no display/audio; keep SDL quiet.
          SDL_VIDEODRIVER: dummy
          SDL_AUDIODRIVER: dummy
        run: uv run pyinstaller standoff.spec --noconfirm

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: Compress-Archive -Path dist/standoff -DestinationPath ${{ matrix.asset }} -Force

      - name: Package (macOS)
        if: runner.os == 'macOS'
        # ditto preserves the .app bundle's symlinks/permissions (zip can mangle them).
        run: ditto -c -k --sequesterRsrc --keepParent "dist/Standoff.app" "${{ matrix.asset }}"

      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: cd dist && zip -r "../${{ matrix.asset }}" standoff

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.asset }}
          path: ${{ matrix.asset }}
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Create release and attach bundles
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/*.zip
          generate_release_notes: true
          fail_on_unmatched_files: true

  publish-itch:
    name: Publish to itch.io
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      # butler diffs/patches at the file level, so unpack each zip rather than
      # pushing it as a single opaque file.
      - name: Unpack per-OS bundles
        run: |
          mkdir -p itch/windows itch/linux itch/osx
          unzip -q artifacts/standoff-windows.zip -d itch/windows
          unzip -q artifacts/standoff-linux.zip -d itch/linux
          unzip -q artifacts/standoff-macos.zip -d itch/osx

      - name: Push Windows build
        uses: yeslayla/butler-publish-itchio-action@v1.2.0
        env:
          BUTLER_CREDENTIALS: ${{ secrets.BUTLER_API_KEY }}
          ITCH_USER: umutcanekinci
          ITCH_GAME: standoff
          CHANNEL: windows
          VERSION: ${{ github.ref_name }}
          PACKAGE: itch/windows

      - name: Push Linux build
        uses: yeslayla/butler-publish-itchio-action@v1.2.0
        env:
          BUTLER_CREDENTIALS: ${{ secrets.BUTLER_API_KEY }}
          ITCH_USER: umutcanekinci
          ITCH_GAME: standoff
          CHANNEL: linux
          VERSION: ${{ github.ref_name }}
          PACKAGE: itch/linux

      - name: Push macOS build
        uses: yeslayla/butler-publish-itchio-action@v1.2.0
        env:
          BUTLER_CREDENTIALS: ${{ secrets.BUTLER_API_KEY }}
          ITCH_USER: umutcanekinci
          ITCH_GAME: standoff
          CHANNEL: osx
          VERSION: ${{ github.ref_name }}
          PACKAGE: itch/osx
